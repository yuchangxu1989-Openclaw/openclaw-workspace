# 评测集清洗质量核查报告（批次1-4）

- 核查人：QA（质量分析）
- 核查时间：2026-03-09 00:12+08
- 依据标准：Feishu《AEO评测标准与基线V3》`JxhNdoc7ko7ZLwxJUJHcWyeDnYd`（已通读）
- 核查范围（全量逐条）：
  1. `00-real-badcases.json`
  2. `01-academic-insight.json`
  3. `02-conversation-insight.json`
  4. `03-global-rename.json`

---

## 一、V3关键判定口径（用于本次核查）

1. 难度仅允许 `C1/C2`（出现 `C3/C4/C5` 直接不合格）。
2. `C2` 必须同时满足：
   - 多轮对话
   - 隐含意图
   - 执行链 ≥4步
   - 跨模块协同
3. C1若过于简单（单轮、显式、执行链短）不应大量占据黄金评测集。
4. 来源必须真实：优先 `real_conversation`；编造/场景合成应剔除或降级。
5. `expected_output`/`expected_behavior` 必须可验证、可审计（可观察检查项）。
6. `multi_turn` 标注需与样本内容一致。
7. 删除量应合理（不能“几乎全删”也不能“明显放水”）。
8. 整体 C2 占比必须 ≥80%。

---

## 二、逐文件核查

## 1) `00-real-badcases.json`

### 基本统计（全量）
- 总条目：42
- 难度分布（按字段 `difficulty`）：
  - C1：12
  - C2：27
  - C3：3（不合规）
- C2占比：27/42 = **64.29%**（不达标，需≥80%）

### 逐维度结论
1. **difficulty合法性**：不合格。存在C3（`BADCASE-DELEGATION-003`, `GR-019`, `GR-028`, `GR-030`不在本文件；本文件内有3条C3）。
2. **C2定义符合性**：部分不合格。多条标成C2但样本描述偏“单事件故障记录”，执行链未体现≥4步跨模块。
3. **C1是否过简单**：有明显简单样本（如若干“口头承诺未执行”“单次遗漏”型），作为黄金集占比偏高。
4. **source真实性**：
   - `source`字段混乱：`synthetic`、`real_conversation`、`auto-harvest-engine`并存；
   - 多条 `data_source=real-conversation` 但 `source=synthetic` 自相矛盾；
   - 存在 `auto-harvest` 来源，需判定是否真实会话衍生，否则不应直接入黄金集。
5. **expected_output可审计性**：多数有 `expected_behavior`，但不少仅原则描述，缺少可执行验真条件（如明确检查命令/状态）。
6. **multi_turn标注准确性**：本文件多数无 `multi_turn` 字段，无法审计一致性（字段缺失本身是质量问题）。
7. **删改量合理性**：就当前内容看，仍残留大量非黄金级条目，说明清洗不充分。
8. **C2占比**：64.29%，不达标。

### 文件结论
- **不合格（需返工）**
- 主要返工点：
  - 清除C3
  - 统一真实来源字段（建议单一 `source=real_conversation`，另设 `ingest_channel`）
  - 补齐 `multi_turn`
  - 严格按C2四条件重判并提纯

---

## 2) `01-academic-insight.json`

### 基本统计（全量）
- 总条目：22
- 难度分布：C2=16，C1=6
- C2占比：16/22 = **72.73%**（不达标）

### 逐维度结论
1. **difficulty合法性**：合格（仅C1/C2）。
2. **C2定义符合性**：部分不合格。前16条大体多轮+隐含意图，但不少 `expected_output` 仍偏“分析回答”，未体现可审计执行链≥4步+跨模块动作。
3. **C1是否过简单**：后6条（AI-021/024/025/027/028/029）明显偏单轮问答，过于基础，不宜放在 `c2-golden`。
4. **source真实性**：存在 `scenario_based`（6条），与V3“真实对话优先、禁止编造”冲突。
5. **expected_output可审计性**：多数是“应输出什么内容”，但缺乏量化验真（如必须引用几条历史上下文、是否调用检索）。
6. **multi_turn标注准确性**：基本一致（前段true，后段false），但与C2定义联动不足（C2应几乎都true且有链路要求）。
7. **删改量合理性**：当前保留了较多 `scenario_based+C1`，说明清洗偏宽松。
8. **C2占比**：72.73%，不达标。

### 文件结论
- **不合格（需返工）**
- 建议：删除/迁移 `scenario_based` 6条至非黄金池；提升C2样本到≥80%。

---

## 3) `02-conversation-insight.json`

### 基本统计（全量）
- 总条目：30
- 难度分布：C2=11，C1=19
- C2占比：11/30 = **36.67%**（严重不达标）

### 逐维度结论
1. **difficulty合法性**：合格（仅C1/C2）。
2. **C2定义符合性**：部分条目可达标（如CI-003/005/008/009/010等），但大量C1留在黄金集，且部分C2仍缺可执行链路描述。
3. **C1是否过简单**：问题显著。大量样本为单句语义理解/情绪识别，属于C1基础能力，不应在C2黄金集占主导。
4. **source真实性**：`scenario_based` 占比高（CI-018后大量），不满足真实对话优先。
5. **expected_output可审计性**：多为高质量文字要求，但验真粒度不足（缺“可被审计的通过标准”）。
6. **multi_turn标注准确性**：存在明显不一致：
   - CI-001/002/004/006/007/013/014/016/017 等 context 明显多轮，但标注为 `multi_turn=false`。
7. **删改量合理性**：当前应大幅删/迁 C1 与 scenario_based；未做充分清洗。
8. **C2占比**：36.67%，严重不达标。

### 文件结论
- **不合格（需返工，优先级高）**

---

## 4) `03-global-rename.json`

### 基本统计（全量）
- 总条目：30
- 难度分布：C2=27，C3=3
- C2占比：27/30 = **90%**（单文件达标）

### 逐维度结论
1. **difficulty合法性**：不合格。含C3（`GR-019`, `GR-028`, `GR-030`）。
2. **C2定义符合性**：多数符合“多步骤+跨模块”，但部分为单轮指令式改名场景，隐含意图强度一般。
3. **C1是否过简单**：无C1，结构上较好。
4. **source真实性**：后半段大量 `scenario_based`，不符合V3来源红线。
5. **expected_output可审计性**：相对较好，多数为可检查步骤清单。
6. **multi_turn标注准确性**：本文件无 `multi_turn` 字段，无法审计（字段缺失问题）。
7. **删改量合理性**：仍保留C3与scenario_based，说明清洗不彻底。
8. **C2占比**：90%达标，但因难度非法与来源问题，仍不通过。

### 文件结论
- **不合格（需返工）**

---

## 三、整体统计与结论

### 全量合并统计（四文件）
- 总条目：42 + 22 + 30 + 30 = **124**
- C2总数：27 + 16 + 11 + 27 = **81**
- 整体C2占比：81/124 = **65.32%**（**未达V3≥80%红线**）
- 非法难度（C3）条目：
  - `00-real-badcases.json`：1条（BADCASE-DELEGATION-003）
  - `03-global-rename.json`：3条（GR-019/028/030）
  - 合计：**4条**
- `scenario_based/synthetic/auto-harvest-engine` 等非纯真实来源占比较高，来源合规性不足。
- `multi_turn` 字段缺失或标注不一致问题广泛存在。

### 总体裁决
- **结论：需返工（不合格）**

### 必改清单（按优先级）
1. **硬性清除非法难度**：全量改为C1/C2，C3/C4/C5一律剔除或重标。
2. **重做C2定级**：严格按“四条件同时满足”重判；不满足的降级或移出黄金集。
3. **来源合规化**：
   - 黄金集仅保留可溯源真实对话（`real_conversation`）；
   - `scenario_based/synthetic/auto-harvest-engine` 移出主集或单独标注为候选集。
4. **补齐结构化验真字段**：统一 `expected_output` 为可审计检查项（可验证动作/证据）。
5. **统一并校验 multi_turn**：全量补字段并做一致性校验。
6. **把整体C2占比拉到≥80%**：建议删降大量C1，补充真实C2样本。

---

## 四、文件级最终判定

- `00-real-badcases.json`：**需返工**
- `01-academic-insight.json`：**需返工**
- `02-conversation-insight.json`：**需返工（高优先级）**
- `03-global-rename.json`：**需返工**

> 最终：**批次1-4整体不通过，需返工后复检。**
