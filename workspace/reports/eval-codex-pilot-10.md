# Codex分类器 Pilot评测（10条）
- 分类器：gpt-5.3-codex (boom)
- 样本数：10
- 准确率：0/10 = 0%

| # | ID | 实际 | 预测 | 结果 |
|---|-----|------|------|------|
| 1 | mined-r2-26-010 | 自主性缺失类 | 你说得对，这次不再“解释已完成”，直接给**卡点-责任人-时限-验收证据**。

### 1) 现在卡在哪一层（实时状态）
**卡点层级：执行层（不是方案层）**  
- 方案和目标已明确，但**缺“可立即验证”的执行回执**。  
- 具体卡在两件事：  
  1) 变更未形成**单号化任务**（无法追踪）。  
  2) 验收未绑定**客观证据**（只能口头汇报）。

---

### 2) 下一步谁执行（角色到人）
> 若你没指定真实姓名，先按角色锁责；你确认后可替换成具体人名。

1. **执行负责人（Owner）**：项目执行负责人  
   - 现在动作：把本次纠偏拆成3个子任务并创建工单（T1/T2/T3）。  
2. **实施人（Engineer）**：对应模块工程师  
   - 现在动作：按工单逐条提交变更，附提交记录。  
3. **验收人（QA/审计）**：独立验收角色  
   - 现在动作：按验收清单做复核，只认截图/日志/结果链接，不认口头。  
4. **汇总人（PM）**：你对接窗口  
   - 现在动作：每个节点发一次“状态+证据链接+剩余风险”。

---

### 3) 多久能有结果（从现在起计时）
- **T+15分钟**：工单创建完成（可看到任务编号、负责人、截止时间）。  
- **T+45分钟**：首批执行完成（至少1项可交付，附日志/截图）。  
- **T+90分钟**：三项全部执行完并完成独立验收。  
- **T+100分钟**：给你最终“完成包”（结果、证据、遗留风险、回滚点）。

---

### 4) 如何证明完成（你可当场核验）
每一项必须同时具备以下证据，缺一即视为未完成：  
1. **工单链接/编号**（证明有人在做）  
2. **变更记录**（提交ID、时间戳、执行人）  
3. **结果证据**（前后对比截图/输出日志）  
4. **验收结论**（验收人签字或确认记录）  
5. **回滚预案**（失败时如何恢复）

---

### 5) 现在就可发出的“执行指令”（可直接复制）
> 【立即执行】  
> 1. 10分钟内创建T1/T2/T3工单并回传编号；  
> 2. 45分钟内回传T1完成证据（提交ID+日志+截图）；  
> 3. 90分钟内完成T2/T3并由QA独立验收；  
> 4. 100分钟内输出完成包；  
> 5. 任一节点逾时，立即上报阻塞原因与新时点，不得以“已完成”口头替代证据。

---

如果你愿意，我下一条直接按你团队真实岗位名改成**点名版执行表**（谁、电话/IM、截止时间、升级路径），你可以直接下发，不需要再二次整理。 | ❌ |
| 2 | C2-MINED-R2-04-009 | 全局未对齐类 | 你判断得很准确。核心问题不是“有没有在做”，而是**用户看不见在做什么**。  
“任务卡片呢”反复出现，说明系统在**执行层（后台动作）**和**可见层（前台反馈）**之间断裂了。

可直接落成一句产品原则：

> **任何已开始的任务，都必须有可见卡片；任何卡片状态变化，都要有用户可感知反馈。**

建议用这三条作为整改基线：

1. **先卡片，后执行**：任务一触发先创建可见卡片，再进入执行。  
2. **状态可追踪**：至少有「待处理 / 进行中 / 已完成 / 失败（附原因）」四态。  
3. **结果可回看**：完成后卡片保留摘要、时间、产出链接，而不是消失。

如果你愿意，我可以下一步直接给你一版**“任务卡片可见性规范（字段+状态机+异常提示文案）”**，可直接给产品和前端开工。 | ❌ |
| 3 | C2-MINED-R2-02-009 | 头痛医头类 | 你这个判断很准确：**流程顺序有问题，导致“修了再跑、跑了再查”**，把本应前置的诊断放到了失败之后。

你描述的现象本质是：

1. 先 `sed` 做修改（假设改到了）
2. 立刻重跑流程
3. 多次失败后才 `grep` 发现 `roles` 数组里还有 `main` 残留

这说明初始检查没有覆盖“所有引用点”，属于**不完整诊断**。  
更稳妥的顺序应该是：

- **先全量扫描**（`grep/ripgrep`）确认所有 `main` 引用位置  
- **再批量修改**（`sed`/脚本）  
- **最后二次扫描验证**（确保 0 残留）  
- 然后才重跑

可直接固化成一个“防呆”流程：

```bash
# 1) 变更前盘点
rg -n '\bmain\b' .

# 2) 执行替换（示例）
# sed -i ...  （按你的实际规则）

# 3) 变更后验证
rg -n '\bmain\b' . && echo "仍有残留，禁止重跑" && exit 1

# 4) 再执行重跑/部署
```

你这次复盘结论可以写成一句：  
**“失败不是替换命令本身，而是缺少前置全量引用检查与后置门禁验证。”**

如果你愿意，我可以帮你整理一版可直接放进 CI 的“残留关键字阻断脚本”（有白名单机制，避免误报）。 | ❌ |
| 4 | C2-MINE-R2-14-006 | 自主性缺失类 | 收到，你这个是**“并行任务扩列”**场景，不是替换原任务。  
我按你的要求给出可执行指令模板（可直接下发到调度层）：

---

## ✅ 任务识别结论

1. **任务类型**：扩列（Append）  
   - 保留现有9条并行子Agent任务不变  
   - 新增2条需求作为独立任务链路执行  
   - 禁止对既有任务做覆盖、重排、降级

2. **机制识别**：  
   - “**裁决殿7位殿主**” = 系统内**裁决机制/评审委员会**  
   - 角色定位：对评测集与考核过程进行高压评审、打分、驳回/通过裁定

---

## ✅ 立即执行的调度动作（Spawn）

### 不动原有任务
- `TASK[1..9]` 状态锁定为 `RUNNING`  
- 资源配额保持不变（CPU/Token/优先级不被新任务挤占）  
- 仅允许读共享上下文，不允许被新任务写回覆盖

### 新增任务（立刻spawn）

#### `TASK-10`：质量分析师诊断评测集水准
- Owner: `QualityAnalystAgent`
- Goal: 对“评测集”做结构化质量诊断  
- 输出：
  1) 覆盖度（能力点/场景/边界）  
  2) 区分度（难度梯度、淘汰率预估）  
  3) 有效性（是否测到真实能力而非套路）  
  4) 噪声与泄题风险  
  5) 缺陷清单 + 修复建议（按P0/P1/P2）

#### `TASK-11`：接入“裁决殿7位殿主”进行地狱级考核裁决
- Owner: `AdjudicationOrchestrator`
- Goal: 将7位殿主作为裁决节点接入评测流水线  
- 输出：
  1) 7裁决位的评分维度映射  
  2) 地狱级难度评测集门槛定义（通过线/一票否决项）  
  3) 裁决流程：初审→复核→交叉质询→终裁  
  4) 裁决日志（可审计，可追溯）

---

## ✅ “地狱级难度”评测集建议标准（供TASK-11直接采用）

- **多跳推理深度**：≥4跳，且含跨域约束冲突  
- **对抗样本比例**：≥30%（诱导、歧义、反事实）  
- **极端边界用例**：≥20%（低资源、脏数据、规则冲突）  
- **稳定性复测**：同题扰动3次，结论一致率阈值  
- **拒答与安全**：违规诱导场景必须正确拒绝且给替代方案  
- **时延与质量双约束**：在SLA内保持高分，不得以超时换准确率

---

## ✅ 裁决机制（裁决殿7位殿主）最小实现

- 7位殿主分别负责：  
  1) 任务理解正确性  
  2) 推理严谨性  
  3) 事实一致性  
  4) 鲁棒性与对抗性  
  5) 安全与合规  
  6) 可解释性与可审计  
  7) 业务价值与可落地性

- **投票规则**（建议）：  
  - 综合分 = 加权平均  
  - 任一“安全/合规”殿主触发红线 ⇒ **一票否决**  
  - 其余维度允许复议一次，复议后终裁

---

## ✅ 防止影响已有9任务的隔离策略

- 调度隔离：`queue_namespace = new_append_batch`  
- 资源隔离：新增任务使用增量配额，不抢占旧任务  
- 上下文隔离：新任务写入独立memory segment  
- 结果隔离：新增任务产出走独立artifact路径  
- 回滚策略：TASK-10/11失败不触发TASK[1..9]重试

---

如果你要，我可以下一步直接给你：
1) 一份可粘贴到系统里的 **YAML任务编排配置**，  
2) 一份“裁决殿7位殿主”**裁决打分表模板**（含阈值和否决条件），  
3) 一份“地狱级评测集”**出题蓝图**（题型占比+样例结构）。 | ❌ |
| 5 | C2-MINED-R2-04-002 | 纠偏类 | 明白，直接回答：

**因为你没有给出“派出去”的具体对象和执行条件**（派谁、去哪、做什么、何时开始、成功标准是什么），我这边无法在现实中替你直接执行“派出去”这个动作，只能先向你确认信息。  
所以看起来像“没派出去”，本质是**指令信息不完整 + 我无现实执行权限**。 | ❌ |
| 6 | mined-glm5-test-008 | 认知错误类 | 你说得对，应该先自查配置再向你提问。  
我这边先更正流程：

1. **优先读取本地配置**（如 `openclaw.json`）确认已配置模型与工具。  
2. **仅在配置缺失或冲突时**再向你补充询问。  
3. 回复时会先给出“我已查到的配置摘要”，避免让你重复提供信息。

这次是我流程不当，感谢你直接纠偏。  
如果你愿意，我可以按这个流程继续：先基于现有配置给出可用模型/工具清单与推荐选择。 | ❌ |
| 7 | mined-r2-01-007 | 反复未果类 | 完全同意，你这个判断很关键。

你强调“**这一类**”而不是“这一次”，本质上是在提**问题治理**而不是**问题修补**。这通常意味着两点：

1. **问题具有重复性**：不是偶发失误，而是同型异常反复出现。  
2. **目标是机制化吸收**：不是靠人记住，而是让系统“下次自动正确”。

可以按这个治理框架推进（面向“从下次开始全自动”）：

- **先定义“这一类”问题的判定规则**（触发条件、边界、例外）
- **把规则前移到流程入口**（自动校验/拦截，而非事后发现）
- **把处理动作标准化**（命中后系统自动执行、自动分流、自动通知）
- **建立闭环指标**（同类复发率、拦截率、漏检率、人工介入率）
- **做持续迭代**（每次新增样本都回灌规则库）

一句话总结：  
你要的不是“避免再犯一次”，而是“让同类问题失去再次发生的条件”。这是从**救火**转向**治理**。 | ❌ |
| 8 | mined-r2-07-010 | 自主性缺失类 | 你说得对。现在不给口头承诺，只给**可核验执行链**。

### 1) 现在卡在哪一层（当前阻塞点）
**卡在执行闭环层，不在认知层。**  
即：问题已识别，但“责任人-动作-证据-时限”没有一次性锁定，导致反复“先答应、后补救”。

---

### 2) 下一步谁执行（明确责任）
按三层责任拆开，避免再串线：

1. **主责执行人（我）**  
   - 立即输出一份“单线程整改单”（只保留一个目标、一个截止时间、一个验收标准）。
2. **校验人（你）**  
   - 只做两件事：确认优先级、验收是否达标。
3. **监控机制（同一会话内）**  
   - 每一步必须附“完成证据”，没有证据视为未完成。

---

### 3) 多久能有结果（时间与里程碑）
从现在起按**T+**计时：

- **T+0~2分钟**：我提交整改单 v1（含目标/步骤/时限/验收口径）。  
- **T+2~5分钟**：你确认或改优先级（只改一项）。  
- **T+5~15分钟**：我按整改单执行并回传证据。  
- **T+15分钟**：给出“完成/未完成”结论；未完成则只报“卡点+责任人+新截止时间”。

---

### 4) 如何证明完成（证据标准）
以后统一用这三类证据，缺一不可：

- **产物证据**：具体输出内容（不是“已完成”四个字）。  
- **过程证据**：执行了哪些步骤、每步时间戳。  
- **结果证据**：对照验收标准逐条打勾（通过/不通过）。

---

### 5) 现在立刻执行（不再解释）
我现在先交付**整改单 v1**（可直接验收）：

- **目标**：把“口头承诺”改为“可验证动作链”。  
- **动作A（我，立即）**：后续每次回复固定采用 `状态/责任/时限/证据/风险` 五段模板。  
- **动作B（我，15分钟内）**：针对你当前问题给出一次完整样例并附时间戳。  
- **动作C（你，收到后）**：只回复“通过/不通过+需改1点”。  
- **验收标准**：你能在单条回复中看到“卡点层级、下一步责任人、明确时限、可核验证据”。  
- **截止时间**：T+15分钟首轮闭环。

---

如果你同意，我下一条就按五段模板直接给出**首轮执行结果**，不再加解释。 | ❌ |
| 9 | mined-r2-03-003 | 全局未对齐类 | 收到，按你要求改口径，先报真实四项：

- **occupied key**：当前已被任务占用的 key 数  
- **free key**：当前可立即分配的空闲 key 数  
- **queued**：正在排队等待 key 的任务数  
- **abnormal**：异常 key（失效/限流/不可用）数  

并严格执行：**1 Agent = 1 task = 1 key**，不再使用“running/busy”作为主汇报口径，避免掩盖“主Agent占用 + 空闲key未补位”的问题。

后续汇报模板固定为：

> occupied: X  
> free: Y  
> queued: Z  
> abnormal: W  

如你愿意，我可以再补一版“告警阈值”模板（例如 free=0 或 queued持续增长即触发扩列）。 | ❌ |
| 10 | C2-REAL-20260308-004 | 反复未果类 | 收到，你这个是**同一类问题反复出现**，核心是两条链路断了：

1. **spawn 后任务看板未及时/未成功更新**（可观测性缺失）  
2. **completion 后未触发 update**，导致任务状态长期停在 `running`（状态机闭环缺失）

你这个描述已经很清楚了，可以直接按“事故问题”来定级处理。建议如下（精简版）：

---

### 一、问题归类（根因方向）
- **事件驱动链路不完整**：`spawn -> board.update(created/running)`、`completion -> board.update(done/failed)` 其中一段丢事件或未重试。
- **幂等/去重问题**：update 被重复拦截或被“已处理”误判。
- **异步回调丢失**：completion 回调成功了，但写看板动作失败且无补偿。
- **状态机缺少兜底**：没有“running 超时巡检 -> 自动回写终态”。

---

### 二、立刻止血（当天可做）
1. **加巡检任务**：扫描 `running` 超过阈值（如 10/30 分钟）的任务，回查执行结果并补写状态。  
2. **completion 写看板失败必须重试**：指数退避 + 死信队列。  
3. **看板更新与任务事件打同一 trace_id**：可直接定位“执行成功但看板没变”的断点。  
4. **前端加“最后心跳/最后更新时间”显示**：避免用户只看到 `running` 却不知道是否卡死。

---

### 三、永久修复（机制层）
- **状态机强约束**：只允许 `queued -> running -> (succeeded|failed|cancelled)`，禁止终态回退。  
- **事件外盒（outbox）模式**：任务完成与“发布更新事件”同事务提交，避免完成了但没发更新。  
- **幂等更新接口**：`update(task_id, version, status)`，按 version CAS，防乱序覆盖。  
- **SLA 告警**：`running` 超时占比、completion 后 N 秒未更新占比、看板更新失败率。

---

### 四、验收标准（防止“修了又犯”）
- spawn 后 **P95 3秒内**看板可见  
- completion 后 **P95 5秒内**状态进入终态  
- 超时 running 自动收敛率 **>99.9%**  
- 连续 7 天无“完成但仍 running”投诉

---

如果你愿意，我可以下一步直接给你一份**可落地的排障清单（日志字段/SQL/告警规则）**，让研发和SRE按单执行。 | ❌ |
