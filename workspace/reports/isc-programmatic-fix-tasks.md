# ISC程序化修复任务清单（B类P0+P1）

以下任务可直接派发给coder子Agent执行。每项需提交：变更文件列表、核心实现、测试与验证命令。

## 任务1: rule.auto-github-sync-trigger-001 auto_github_sync
- 目标：将规则 rule.auto-github-sync-trigger-001 程序化落地，接入git hook（pre-commit/post-commit）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务2: rule.git-commit-dispatch-001 
- 目标：将规则 rule.git-commit-dispatch-001 程序化落地，接入git hook（pre-commit/post-commit）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务3: rule.intent-ic4-ic5-boundary-001.json IC4/IC5意图边界判定规则
- 目标：将规则 rule.intent-ic4-ic5-boundary-001.json 程序化落地，接入git hook（pre-commit/post-commit）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务4: rule.intent-post-commit-quality-gate-h8z2sz intent_post-commit-quality-gate
- 目标：将规则 rule.intent-post-commit-quality-gate-h8z2sz 程序化落地，接入git hook（pre-commit/post-commit）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务5: rule.isc-skill-permission-classification-031 技能权限分级体系
- 目标：将规则 rule.isc-skill-permission-classification-031 程序化落地，接入git hook（pre-commit/post-commit）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务6: rule.memory-digest-must-verify-001 
- 目标：将规则 rule.memory-digest-must-verify-001 程序化落地，接入git hook（pre-commit/post-commit）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务7: rule.self-correction-to-rule-001 缺陷根因分析与修复
- 目标：将规则 rule.self-correction-to-rule-001 程序化落地，接入git hook（pre-commit/post-commit）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务8: skill.evolution.auto-trigger 技能进化自动触发
- 目标：将规则 skill.evolution.auto-trigger 程序化落地，接入git hook（pre-commit/post-commit）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务9: N007-v2 source_file_delivery_method
- 目标：将规则 N007-v2 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务10: dispatch-auto-report-001 批量派发后自动汇报
- 目标：将规则 dispatch-auto-report-001 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务11: rule.architecture-diagram-visual-output-001 
- 目标：将规则 rule.architecture-diagram-visual-output-001 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务12: rule.cras-dual-channel-001 
- 目标：将规则 rule.cras-dual-channel-001 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务13: rule.detection-report-feishu-card-001 report_feishu_card_format
- 目标：将规则 rule.detection-report-feishu-card-001 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务14: rule.intent-pdf输出标准-fyhznt intent_pdf输出标准
- 目标：将规则 rule.intent-pdf输出标准-fyhznt 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务15: rule.isc-creation-gate-001 isc_creation_validation_gate
- 目标：将规则 rule.isc-creation-gate-001 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务16: rule.n020-auto-universal-root-cause-analysis-020 universal_root_cause_and_gap_analysis
- 目标：将规则 rule.n020-auto-universal-root-cause-analysis-020 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务17: rule.n022-detection-architecture-design-isc-compliance-audit-022 architecture_design_isc_compliance_audit
- 目标：将规则 rule.n022-detection-architecture-design-isc-compliance-audit-022 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务18: rule.n033-gateway-config-protection 
- 目标：将规则 rule.n033-gateway-config-protection 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务19: rule.n036-memory-loss-recovery memory_loss_self_recovery
- 目标：将规则 rule.n036-memory-loss-recovery 程序化落地，接入startup自检。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务20: rule.skill-distribution-auto-classify-001 技能本地/可销售自动分类
- 目标：将规则 rule.skill-distribution-auto-classify-001 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务21: rule.sprint-closure-acceptance-001.json Sprint收工验收门禁
- 目标：将规则 rule.sprint-closure-acceptance-001.json 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务22: rule.task-orchestration-quality-001.json 
- 目标：将规则 rule.task-orchestration-quality-001.json 程序化落地，接入startup自检。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务23: rule.threshold-alert-routing-001 
- 目标：将规则 rule.threshold-alert-routing-001 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务24: rule.tracker-sync-gate-001.json PROJECT-TRACKER 同步门禁
- 目标：将规则 rule.tracker-sync-gate-001.json 程序化落地，接入startup自检。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务25: rule.vectorization-auto-trigger-001 resource_lifecycle_auto_vectorization
- 目标：将规则 rule.vectorization-auto-trigger-001 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0

## 任务26: rule.visual-output-style-001 
- 目标：将规则 rule.visual-output-style-001 程序化落地，接入事件监听（completion事件等）。
- 交付要求：
  1) 新增/修改执行脚本（Python/Node/Bash）实现规则检测与失败返回码。
  2) 接入对应触发机制（hook/事件监听/startup）。
  3) 增加最小可复现测试样例（正例+反例）。
  4) 更新README或rules索引，注明自动化入口与回滚方式。
- 验收命令：
  - 运行自动化脚本应输出结构化结果（JSON或明确日志）
  - 违规样例返回非0，合规样例返回0
