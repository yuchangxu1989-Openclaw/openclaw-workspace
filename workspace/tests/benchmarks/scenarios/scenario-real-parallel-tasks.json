{
  "id": "scenario-real-parallel-tasks",
  "name": "多任务并行：三路诊断同时跑",
  "domain": "development",
  "description": "用户要求同时执行多个独立诊断任务并汇总结果",
  "source": "real-conversation",
  "source_date": "2026-03-03",
  "original_text": "三路并行诊断：闭环完整性/代码质量/配置健康度，报告写入reports/",
  "complexity": {
    "turns": "single-turn",
    "intent": "multi-intent",
    "context": "context-free",
    "interrupt": "no-interrupt",
    "domain_cross": "cross-domain"
  },
  "complexity_tags": ["multi-intent", "parallel-execution", "cross-domain", "result-aggregation"],
  "steps": [
    {
      "action": "emit",
      "event": "user.message",
      "payload": { "text": "帮我跑三路并行诊断：1.系统闭环完整性 2.核心代码质量 3.配置健康度，结果写reports/" }
    },
    { "expect": "intent_detected", "intent_category": "IC3", "timeout_ms": 5000 },
    { "expect": "rule_matched", "min_rules": 1 },
    { "expect": "dispatched", "handler_pattern": "*" },
    { "expect": "result", "has_output": true }
  ]
}
