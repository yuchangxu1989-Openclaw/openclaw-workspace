{
  "id": "scenario-real-config-validation",
  "name": "多轮配置变更：fallback容灾三步走",
  "domain": "development",
  "description": "多步骤配置变更，每步需要验证后才能继续，中间遇到格式错误需回滚",
  "source": "real-conversation",
  "source_date": "2026-03-04",
  "original_text": "先加cherryin备选→验证→再加zhipu→验证→第一次失败format错误→回滚→重做",
  "complexity": {
    "turns": "multi-turn",
    "intent": "single-intent",
    "context": "context-dependent",
    "interrupt": "system-interrupt",
    "domain_cross": "single-domain"
  },
  "complexity_tags": ["multi-turn", "context-dependent", "system-interrupt", "validation-loop", "rollback"],
  "steps": [
    {
      "action": "emit",
      "event": "user.message",
      "payload": { "text": "帮我配置fallback容灾：主力penguinsaichat，备选cherryin和zhipu，分步验证" }
    },
    { "expect": "intent_detected", "intent_category": "IC2", "timeout_ms": 5000 },
    { "expect": "rule_matched", "min_rules": 1 },
    { "expect": "dispatched", "handler_pattern": "*" },
    { "expect": "result", "has_output": true }
  ]
}
