{
  "id": "scenario-complex-approval-and-extend",
  "name": "认可+扩展：行就这样对了再帮我...",
  "domain": "development",
  "description": "IC5复合意图：先认可当前成果，然后追加新任务",
  "source": "synthetic",
  "reference": "IC5 composite intent from intent benchmark",
  "complexity": {
    "turns": "single-turn",
    "intent": "multi-intent",
    "context": "context-dependent",
    "interrupt": "no-interrupt",
    "domain_cross": "cross-domain"
  },
  "complexity_tags": ["multi-intent", "composite-intent", "approval-plus-extend", "context-dependent"],
  "steps": [
    {
      "action": "emit",
      "event": "user.message",
      "payload": { "text": "事件总线实现得不错，按这个方向走。另外把Dispatcher的路由表也改了，加上handler热重载" }
    },
    { "expect": "intent_detected", "timeout_ms": 5000 },
    { "expect": "rule_matched", "min_rules": 1 },
    { "expect": "dispatched", "handler_pattern": "*" },
    { "expect": "result", "has_output": true }
  ]
}
