{
  "id": "scenario-real-iterative-design",
  "name": "迭代式设计精炼：v1到v4.3",
  "domain": "development",
  "description": "多轮架构设计迭代，每轮有评审反馈，自动打回再改进",
  "source": "real-conversation",
  "source_date": "2026-03-04",
  "original_text": "v4→工程师2致命风险→v4.1→质量分析师7.08→v4.2→两份报告分歧→v4.3",
  "complexity": {
    "turns": "session-spanning",
    "intent": "single-intent",
    "context": "context-dependent",
    "interrupt": "no-interrupt",
    "domain_cross": "single-domain"
  },
  "complexity_tags": ["session-spanning", "context-dependent", "iterative-refinement", "multi-agent-review"],
  "steps": [
    {
      "action": "emit",
      "event": "user.message",
      "payload": { "text": "设计ISC-事件-DTO闭环方案，要基于77条现有规则，支持运行时绑定" }
    },
    { "expect": "intent_detected", "intent_category": "IC2", "timeout_ms": 5000 },
    { "expect": "rule_matched", "min_rules": 1 },
    { "expect": "dispatched", "handler_pattern": "*" },
    { "expect": "result", "has_output": true }
  ]
}
