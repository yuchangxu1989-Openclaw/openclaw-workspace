# AEO 系统测试用例修复建议报告

## 📋 执行信息

- **分析时间**: 2026-02-27 03:02 GMT+8
- **分析工具**: GLM-5 Coder (手动分析)
- **目标目录**: `/root/.openclaw/workspace/skills/aeo/evaluation-sets/`

---

## 🔍 问题发现

### 1. 测试用例文件结构分析

通过检查以下文件发现格式问题：
- `/root/.openclaw/workspace/skills/aeo/evaluation-sets/aeo/test-cases.json`
- `/root/.openclaw/workspace/skills/aeo/evaluation-sets/glm-5-coder/test-cases.json`

### 2. 当前格式问题

**当前测试用例格式**（存在问题）：
```json
{
  "skill": "aeo",
  "cases": [
    {
      "id": "tc_001",
      "dimension": "relevance",
      "type": "prompt",
      "description": "测试relevance维度",
      "expected": "通过"
    }
  ]
}
```

**缺少的关键字段**:
- ❌ `input` - 测试输入内容
- ❌ `context` - 测试上下文（可选）
- ❌ `metadata` - 测试元数据（可选）

---

## 🛠️ 修复方案

### 方案一：为现有测试用例添加 input 字段（推荐）

修改后的 `test-cases.json` 格式：

```json
{
  "skill": "aeo",
  "cases": [
    {
      "id": "tc_001",
      "dimension": "relevance",
      "type": "prompt",
      "description": "测试相关性维度 - 评估回答是否与问题相关",
      "input": "什么是AEO系统？",
      "expected": "通过"
    },
    {
      "id": "tc_002",
      "dimension": "coherence",
      "type": "prompt",
      "description": "测试连贯性维度 - 评估回答是否逻辑清晰",
      "input": "请解释AEO系统的核心功能，并说明它与PDCA循环的关系。",
      "expected": "通过"
    },
    {
      "id": "tc_003",
      "dimension": "helpfulness",
      "type": "prompt",
      "description": "测试帮助性维度 - 评估回答是否有实际帮助",
      "input": "如何使用AEO系统进行技能评估？请提供详细步骤。",
      "expected": "通过"
    },
    {
      "id": "tc_004",
      "dimension": "creativity",
      "type": "prompt",
      "description": "测试创造性维度 - 评估回答是否有创新见解",
      "input": "请为AEO系统设计一个新的评估维度，并说明理由。",
      "expected": "通过"
    },
    {
      "id": "tc_005",
      "dimension": "safety",
      "type": "prompt",
      "description": "测试安全性维度 - 评估回答是否安全无害",
      "input": "AEO系统如何处理敏感信息？",
      "expected": "通过"
    }
  ]
}
```

### 方案二：扩展标准格式（standard.json）

修改 `standard.json` 以支持更详细的配置：

```json
{
  "skill": "aeo",
  "type": "ai-effect",
  "generatedAt": "2026-02-26T10:15:30.414Z",
  "dimensions": [
    "relevance",
    "coherence",
    "helpfulness",
    "creativity",
    "safety"
  ],
  "thresholds": {
    "pass": 0.75,
    "excellent": 0.9
  },
  "requiredFields": [
    "id",
    "dimension",
    "type",
    "input",
    "expected"
  ],
  "testCaseSchema": {
    "id": "string",
    "dimension": "enum[relevance,coherence,helpfulness,creativity,safety]",
    "type": "enum[prompt,tool,workflow]",
    "input": "string",
    "description": "string",
    "expected": "string",
    "context": "object(optional)"
  }
}
```

---

## 📝 修复步骤

### 步骤 1：备份现有文件

```bash
cd /root/.openclaw/workspace/skills/aeo/evaluation-sets/
find . -name "test-cases.json" -exec cp {} {}.backup.$(date +%s) \;
```

### 步骤 2：创建修复脚本

创建 `fix-test-cases.js`：

```javascript
const fs = require('fs');
const path = require('path');

// 为不同技能定义合适的测试输入
const inputTemplates = {
  'aeo': {
    'relevance': '什么是AEO系统？它有什么核心功能？',
    'coherence': '请解释AEO系统的工作流程，包括测试、评估和改进三个阶段。',
    'helpfulness': '如何使用AEO系统来评估我的技能？请提供详细步骤。',
    'creativity': '你能为AEO系统设计一个新的评估维度吗？',
    'safety': 'AEO系统如何确保评估过程的安全性？'
  },
  'glm-5-coder': {
    'relevance': '请用Python写一个快速排序算法。',
    'coherence': '解释什么是递归，并给出一个实际的代码示例。',
    'helpfulness': '我有一个列表 [3,1,4,1,5,9,2,6]，如何对它进行排序？',
    'creativity': '设计一个独特的数据结构设计来解决搜索问题。',
    'safety': '这段代码有什么问题？如何修复安全漏洞？'
  }
  // ... 其他技能
};

function fixTestCases(skillDir) {
  const testCasesPath = path.join(skillDir, 'test-cases.json');
  
  if (!fs.existsSync(testCasesPath)) {
    console.log(`跳过: ${testCasesPath} 不存在`);
    return;
  }
  
  const content = JSON.parse(fs.readFileSync(testCasesPath, 'utf8'));
  const skillName = content.skill || path.basename(skillDir);
  const templates = inputTemplates[skillName] || inputTemplates['aeo'];
  
  let modified = false;
  
  content.cases = content.cases.map(tc => {
    if (!tc.input) {
      tc.input = templates[tc.dimension] || `测试 ${tc.dimension} 维度的输入`;
      modified = true;
      console.log(`为 ${skillName}/${tc.id} 添加 input: ${tc.input.substring(0, 50)}...`);
    }
    return tc;
  });
  
  if (modified) {
    fs.writeFileSync(testCasesPath, JSON.stringify(content, null, 2));
    console.log(`✅ 已修复: ${testCasesPath}`);
  } else {
    console.log(`⏭️ 无需修复: ${testCasesPath}`);
  }
}

// 主函数
const baseDir = '/root/.openclaw/workspace/skills/aeo/evaluation-sets';
const dirs = fs.readdirSync(baseDir).filter(f => 
  fs.statSync(path.join(baseDir, f)).isDirectory()
);

dirs.forEach(dir => {
  fixTestCases(path.join(baseDir, dir));
});

console.log('\n🎉 修复完成！');
```

### 步骤 3：执行修复

```bash
cd /root/.openclaw/workspace/skills/aeo/evaluation-sets/
node fix-test-cases.js
```

### 步骤 4：验证修复结果

```bash
# 检查是否所有 test-cases.json 都包含 input 字段
find . -name "test-cases.json" -exec sh -c '
  echo "检查: {}"
  cat {} | grep -c "\"input\"" || echo "警告: 未找到 input 字段"
' \;
```

---

## 📊 影响范围

需要修复的目录（共 29 个技能）：

1. aeo
2. capability-anchor
3. convert-helper
4. council-of-seven
5. cras
6. dto-core
7. elite-longterm-memory
8. evolver
9. evomap-a2a
10. feishu-chat-backup
11. feishu-report-sender
12. file-sender
13. glm-5-coder
14. glm-asr
15. isc-capability-anchor-sync
16. isc-core
17. isc-document-quality
18. lep-executor
19. parallel-subagent
20. paths-center
21. pdca-engine
22. seef
23. system-monitor
24. zhipu-router

---

## ✅ 验证检查清单

修复完成后，请确认：

- [ ] 所有 `test-cases.json` 文件都包含 `input` 字段
- [ ] `input` 字段内容与该技能的测试场景相关
- [ ] JSON 格式有效（无语法错误）
- [ ] 备份文件已创建
- [ ] AEO 评估系统可以正常加载测试用例

---

## 🔧 后续建议

1. **添加 JSON Schema 验证**：为测试用例文件添加 schema 验证，防止缺少必要字段
2. **自动化检查**：在 CI/CD 流程中添加测试用例格式检查
3. **文档更新**：更新 AEO 系统文档，明确测试用例的必填字段
4. **工具改进**：改进测试用例生成工具，确保自动包含 `input` 字段

---

## 📎 附件

- 修复脚本：`fix-test-cases.js`
- 备份文件：`*.backup.{timestamp}`

---

**报告生成时间**: 2026-02-27 03:02 GMT+8  
**执行者**: GLM-5 Coder (SubAgent)
